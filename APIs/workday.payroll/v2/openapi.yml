openapi: 3.0.1
info:
  title: payroll
  description: >-
    This is [WorkDay payroll REST API v2](https://community.workday.com/sites/default/files/file-hosting/restapi/index.html) OpenAPI specification.
    The Payroll service enables you to access and manage payroll information,such as pay groups, payroll inputs, and tax rates.
  x-ballerina-init-description: >
    Follow [this guide] https://credentials.workday.com/docs/authentication/ and
    obtain tokens.
  x-ballerina-display:
    label: Workday Payroll
    iconPath: icon.png
  version: v2
tags:
- name: jobs
- name: payGroupDetails
  description: Provides the ability to view pay group details. Inactive and External
    Pay Group Details are not supported.
- name: payGroups
  description: Provides the ability to view pay groups. Inactive and External Pay
    Groups are not supported.
- name: payrollInputs
  description: "Provides the ability to get, add, update, and delete payroll inputs\
    \ for workers and pay components. These REST APIs: \n\n* Are secured to the Worker\
    \ Data: Payroll Public API (Payroll Input) and Self-Service: Payroll Public API\
    \ (Payroll Input) security domains. \n\n* The POST, PATCH, DELETE methods for\
    \ self-service users require Pay Component Security Segments. Self-service users\
    \ can view their own Payroll Inputs using GET without a Pay Component Security\
    \ Segment. \n\n* Pay Component Segment Security is only supported on the Self-Service:\
    \ Payroll Public API (Payroll Input) security domain. The Segment-Based Security\
    \ Group must use a <b>Self-Service – Worker</b> security group type as the basis.\
    \ \n\n* Don't support Payroll Tax Location Mapping or Pay Components that require\
    \ Special Entry."
- name: taxRates
  description: Use these REST APIs to access company SUI tax rates.
- name: Prompt Values
  description: Retrieves instances that can be used as values for other endpoint parameters
    in this service.
paths:
  /jobs/{id}/payGroup/{subresourceID}:
    get:
      tags:
      - jobs
      summary: Retrieves a single pay group instance.
      operationId: getPayGroupBySubResourceID
      description: |-
        Retrieves a pay group instance with the specified ID.

        Secured by: Set Up: Payroll - Pay Group Specific

        Scope: Core Payroll
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      - name: subresourceID
        in: path
        description: The Workday ID of the subresource.
        required: true
        schema:
          type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payGroupViewDetail'
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /payGroups/{id}:
    get:
      tags:
      - payGroups
      summary: Retrieves a single pay group instance.
      operationId: getPayGroupByID
      description: |-
        Retrieves a pay group with the specified ID.

        Secured by: Set Up: Payroll - Pay Group Specific

        Scope: Core Payroll
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payGroupViewDetail'
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /jobs/{id}/payGroup:
    get:
      tags:
      - jobs
      summary: Retrieves the pay group for a specified job ID. This method always
        returns 1 pay group.
      operationId: getPayGroupByJobID
      description: |-
        Retrieves the pay group for a specified job ID.

        Secured by: Set Up: Payroll - Pay Group Specific

        Scope: Core Payroll
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      - name: effective
        in: query
        description: The effective date of the pay group, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: limit
        in: query
        description: The maximum number of objects in a single response. The default
          is 20. The maximum is 100.
        schema:
          type: integer
          format: int64
      - name: offset
        in: query
        description: 'The zero-based index of the first object in a response collection.
          The default is 0. Use offset with the limit parameter to control paging
          of a response collection. Example: If limit is 5 and offset is 9, the response
          returns a collection of 5 objects starting with the 10th object.'
        schema:
          type: integer
          format: int64
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payGroupViewDetail'
                  total:
                    type: integer
                    format: int64
                description: collection something or other
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /values/taxRatesGroup/companyInstances/:
    get:
      tags:
      - Prompt Values
      description: Retrieves instances that can be used as values for other endpoint
        parameters in this service.
      operationId: getInstanceForEndpointParameters
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/MULTIPLE_INSTANCE_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /values/payrollInputsGroup/worktags/:
    get:
      tags:
      - Prompt Values
      description: Retrieves instances that can be used as values for other endpoint
        parameters in this service.
      operationId: getWorkTagInstancesForEndpointParameters
      parameters:
      - name: payComponent
        in: query
        description: The pay component for the worktags. You can use a returned id
          from GET/values/payrollInputsGroup/payComponents.
        schema:
          type: string
      - name: worker
        in: query
        description: The Workday ID of the worker. You can use a returned id from
          GET /workers in the Staffing service.
        schema:
          type: string
      - name: startDate
        in: query
        description: The start date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: endDate
        in: query
        description: The end date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/MULTIPLE_INSTANCE_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /payGroupDetails/{id}:
    get:
      tags:
      - payGroupDetails
      summary: Retrieves a single pay group detail instance.
      operationId: getPayGroupDetailsByID
      description: |-
        Retrieves a pay group detail with the specified ID.

        Secured by: Set Up: Payroll - Pay Group Specific

        Scope: Core Payroll
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payGroupDetailView'
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /jobs:
    get:
      tags:
      - jobs
      summary: Retrieves a collection of jobs.
      operationId: getCollectionOfJobs
      description: |-
        Retrieves a collection of jobs.

        Secured by: Worker Position: View

        Scope: Staffing
      parameters:
      - name: limit
        in: query
        description: The maximum number of objects in a single response. The default
          is 20. The maximum is 100.
        schema:
          type: integer
          format: int64
      - name: offset
        in: query
        description: 'The zero-based index of the first object in a response collection.
          The default is 0. Use offset with the limit parameter to control paging
          of a response collection. Example: If limit is 5 and offset is 9, the response
          returns a collection of 5 objects starting with the 10th object.'
        schema:
          type: integer
          format: int64
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/jobData'
                  total:
                    type: integer
                    format: int64
                description: collection something or other
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /values/payrollInputsGroup/runCategories/:
    get:
      tags:
      - Prompt Values
      description: Retrieves instances that can be used as values for other endpoint
        parameters in this service.
      operationId: getRunCategoriesForEndpointParameters
      parameters:
      - name: payComponent
        in: query
        description: The pay component for the worktags. You can use a returned id
          from GET/values/payrollInputsGroup/payComponents.
        schema:
          type: string
      - name: worker
        in: query
        description: The Workday ID of the worker. You can use a returned id from
          GET /workers in the Staffing service.
        schema:
          type: string
      - name: startDate
        in: query
        description: The start date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: endDate
        in: query
        description: The end date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/MULTIPLE_INSTANCE_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /payGroupDetails:
    get:
      tags:
      - payGroupDetails
      summary: Retrieves a collection of pay group details.
      operationId: getCollectionOfPayGroupDetails
      description: |-
        Retrieves all pay group details. Inactive and External Pay Group Details are not supported.

        Secured by: Set Up: Payroll - Pay Group Specific

        Scope: Core Payroll
      parameters:
      - name: limit
        in: query
        description: The maximum number of objects in a single response. The default
          is 20. The maximum is 100.
        schema:
          type: integer
          format: int64
      - name: offset
        in: query
        description: 'The zero-based index of the first object in a response collection.
          The default is 0. Use offset with the limit parameter to control paging
          of a response collection. Example: If limit is 5 and offset is 9, the response
          returns a collection of 5 objects starting with the 10th object.'
        schema:
          type: integer
          format: int64
      - name: runCategories
        in: query
        description: 'One or more Workday IDs of run categories for the pay group.
          You can use returned ids from GET /values/payrollInputsGroup/runCategories.You
          can specify 1 or more runCategories query parameters, example: runCategories=category1&runCategories=category2'
        style: form
        explode: true
        schema:
          type: array
          items:
            type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payGroupDetailView'
                  total:
                    type: integer
                    format: int64
                description: collection something or other
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /jobs/{id}:
    get:
      tags:
      - jobs
      summary: Retrieves a single job instance.
      operationId: getSingleJobByID
      description: |-
        Retrieves a job with the specified job ID.

        Secured by: Worker Position: View

        Scope: Staffing
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/jobData'
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /values/payrollInputsGroup/positions/:
    get:
      tags:
      - Prompt Values
      description: Retrieves instances that can be used as values for other endpoint
        parameters in this service.
      operationId: getPositionInstancesForOtherEndpoints
      parameters:
      - name: payComponent
        in: query
        description: The pay component for the worktags. You can use a returned id
          from GET/values/payrollInputsGroup/payComponents.
        schema:
          type: string
      - name: worker
        in: query
        description: The Workday ID of the worker. You can use a returned id from
          GET /workers in the Staffing service.
        schema:
          type: string
      - name: startDate
        in: query
        description: The start date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: endDate
        in: query
        description: The end date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/MULTIPLE_INSTANCE_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /payrollInputs/{id}:
    get:
      tags:
      - payrollInputs
      summary: Retrieves a single payroll input instance.
      operationId: getPayRollInputInstance
      description: |-
        Retrieves a payroll input with the specified ID.

        Secured by: Self-Service: Payroll Public API (Payroll Input), Worker Data: Payroll Public API (Payroll Input)

        Scope: Core Payroll
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payrollInputView'
        400:
          description: |-
            Invalid request. (https://community.workday.com/rest/error-messages)
            Error Code | Validation Message
            --- | ---
            A552 | Specify a Start Date.
            A116 | Specify these payroll worktag types for [pc]: [worktags].
            A119 | Specify an End Date that is on or after the Start Date.
            A551 | Specify a Related Calculation that's valid for [pc].
            A137 | Specify valid run categories. Invalid run categories are: [runcats]
            A560 | Specify a position for [pc].
            A548 | Specify a Pay Component configured with the correct Worker country.
            A550 | Specify a Type for each Input Details row.
            A559 | Specify an End Date for One-time Payroll Input.
            A139 | Specify worktags valid for payroll. Invalid worktags are: [worktags].
            A555 | Specify a Pay Component.
            A98 | Specify only 1 Worktag for each worktag type.
            A138 | Specify a Position that's valid for [worker].
            A940 | Specify a valid level 1 project plan phase. This phase isn't supported: [phase]
            A142 | Enable Input Allowed on [pc] - [pcrc].
            A141 | Specify only 1 Input Details row for each input Type.
            A120 | Enable Input Amount Allowed on [pc].
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
    delete:
      tags:
      - payrollInputs
      summary: Deletes an existing payroll input instance.
      operationId: deleteExistingPayroll
      description: "Deletes an existing payroll input instance with the specified\
        \ ID. \n\nThe Pay Component Segment Security configuration is required. Note\
        \ that Pay Component Segment Security is supported only on the Self-Service:\
        \ Payroll Public API (Payroll Input) security domain. The Segment-Based Security\
        \ Group must use a <b>Self-Service – Worker</b> security group type as the\
        \ basis.\n\nSecured by: Self-Service: Payroll Public API (Payroll Input),\
        \ Worker Data: Payroll Public API (Payroll Input)\n\nScope: Core Payroll"
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data. Deleting resource.
          content: {}
        400:
          description: |-
            Invalid request. (https://community.workday.com/rest/error-messages)
            Error Code | Validation Message
            --- | ---
            A552 | Specify a Start Date.
            A116 | Specify these payroll worktag types for [pc]: [worktags].
            A119 | Specify an End Date that is on or after the Start Date.
            A551 | Specify a Related Calculation that's valid for [pc].
            A554 | You can't delete a payroll input that's used in an off-cycle payment.
            A137 | Specify valid run categories. Invalid run categories are: [runcats]
            A560 | Specify a position for [pc].
            A548 | Specify a Pay Component configured with the correct Worker country.
            A550 | Specify a Type for each Input Details row.
            A556 | You can't delete an external payroll input.
            A559 | Specify an End Date for One-time Payroll Input.
            A139 | Specify worktags valid for payroll. Invalid worktags are: [worktags].
            A555 | Specify a Pay Component.
            A98 | Specify only 1 Worktag for each worktag type.
            A140 | You can't delete a payroll input that's used in a completed payroll result.
            A561 | [pc] isn't configured for self-service. You can't delete your own payroll input.
            A558 | You can't delete a payroll input that's used in a completed or suspended retro result.
            A138 | Specify a Position that's valid for [worker].
            A940 | Specify a valid level 1 project plan phase. This phase isn't supported: [phase]
            A142 | Enable Input Allowed on [pc] - [pcrc].
            A141 | Specify only 1 Input Details row for each input Type.
            A120 | Enable Input Amount Allowed on [pc].
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
    patch:
      tags:
      - payrollInputs
      summary: Partially updates an existing payroll input instance.
      operationId: UpdateAnExistingPayroll
      description: "Partially updates an existing payroll input instance with the\
        \ specified ID and the specified data in the request body. Before calling\
        \ this PATCH method, call the GET /payrollInputs method to retrieve the current\
        \ payroll input information.\n\nYou cannot update the worker field with this\
        \ PATCH method.   \n\nThe Pay Component Segment Security configuration is\
        \ required. Note that Pay Component Segment Security is supported only on\
        \ the Self-Service: Payroll Public API (Payroll Input) security domain. The\
        \ Segment-Based Security Group must use a <b>Self-Service – Worker</b> security\
        \ group type as the basis.\n\n**Starting in v2:**\nThe fieldEditability field\
        \ in the GET /payrollInputs/{ID} response determines the fields that you can\
        \ update with the PATCH /payrollInputs method. Possible fieldEditability values:\
        \  \n\n* all - All fields can be updated, except the worker field.   \n\n\
        * none - No fields can be updated.   \n\n* endDateOnly - Only the endDate\
        \ field can be updated.   \n\n** In v1: **\n\nThe lastPeriodCompleted and\
        \ usedInCompletedResult fields in the GET /payrollInputs/{ID} response determine\
        \ the fields you can update with this PATCH method:  \n\n* If lastPeriodCompleted\
        \ and usedInCompletedResult are both false, all fields can be updated, except\
        \ the worker field.  \n\n* If lastPeriodCompleted and usedInCompletedResult\
        \ are both true, no fields can be updated.  \n\n* If only usedInCompletedResult\
        \ is true, only the endDate field can be updated.\n\nSecured by: Self-Service:\
        \ Payroll Public API (Payroll Input), Worker Data: Payroll Public API (Payroll\
        \ Input)\n\nScope: Core Payroll"
      parameters:
      - name: id
        in: path
        description: The Workday ID of the resource.
        required: true
        schema:
          type: string
      requestBody:
        content:
          'application/json':
            schema:
              $ref: '#/components/schemas/payrollInputView'
        required: true
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data. Updating resource.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payrollInputView'
        400:
          description: |-
            Invalid request. (https://community.workday.com/rest/error-messages)
            Error Code | Validation Message
            --- | ---
            A552 | Specify a Start Date.
            A551 | Specify a Related Calculation that's valid for [pc].
            A13 | You can't include the worker.
            A561 | [pc] isn't configured for self-service. You can't update your own payroll input.
            A121 | Specify an End Date on or after the end date of the last completed pay period for the appropriate run category.
            A548 | Specify a Pay Component configured with the correct Worker country.
            A550 | Specify a Type for each Input Details row.
            A557 | You can't change any fields because the pay input is already used in a completed pay result or retro result.
            A555 | Specify a Pay Component.
            A98 | Specify only 1 Worktag for each worktag type.
            A138 | Specify a Position that's valid for [worker].
            A142 | Enable Input Allowed on [pc] - [pcrc].
            A553 | You can only change the end date because the pay input is already in use in a completed pay result or retro result.
            A116 | Specify these payroll worktag types for [pc]: [worktags].
            A119 | Specify an End Date that is on or after the Start Date.
            A556 | You can't update an external payroll input.
            A137 | Specify valid run categories. Invalid run categories are: [runcats]
            A560 | Specify a position for [pc].
            A140 | You can't change any fields because the pay input is already used in a completed pay result or retro result.
            A994 | You can only update this payroll input with the payroll input lines that are associated with it. Change these payroll input lines: [input line IDs]
            A559 | Specify an End Date for One-time Payroll Input.
            A139 | Specify worktags valid for payroll. Invalid worktags are: [worktags].
            A940 | Specify a valid level 1 project plan phase. This phase isn't supported: [phase]
            A554 | You can't update a payroll input that's used in an off-cycle payment.
            A141 | Specify only 1 Input Details row for each input Type.
            A120 | Enable Input Amount Allowed on [pc].
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
      x-codegen-request-body-name: payrollInputView
  /payrollInputs:
    get:
      tags:
      - payrollInputs
      summary: Retrieves a collection of payroll inputs. At least 1 query parameter
        is required to get data.
      operationId: getCollectionOfPayroll
      description: "Retrieves all payroll inputs. At least 1 query parameter is required\
        \ to get data. \n\nThe Pay Component Segment Security configuration doesn't\
        \ apply to the GET method. Self-service users can view all of their own Payroll\
        \ Inputs.\n\n**Starting in v2:**\nThe fieldEditability field in the GET /payrollInputs/{ID}\
        \ response determines the fields that you can update with the PATCH /payrollInputs\
        \ method. Possible fieldEditability values:  \n\n* all - All fields can be\
        \ updated, except the worker field.   \n\n* none - No fields can be updated.\
        \   \n\n* endDateOnly - Only the endDate field can be updated.   \n\n** In\
        \ v1: **\nThe lastPeriodCompleted and usedInCompletedResult fields in the\
        \ GET /payrollInputs/{ID} response determine the fields that you can update\
        \ with the PATCH /payrollInputs method.   \n\n* If lastPeriodCompleted and\
        \ usedInCompletedResult are both false, all fields can be updated, except\
        \ the worker field.   \n\n* If lastPeriodCompleted and usedInCompletedResult\
        \ are both true, no fields can be updated.   \n\n* If only usedInCompletedResult\
        \ is true, only the endDate field can be updated.\n\nSecured by: Self-Service:\
        \ Payroll Public API (Payroll Input), Worker Data: Payroll Public API (Payroll\
        \ Input)\n\nScope: Core Payroll"
      parameters:
      - name: endDate
        in: query
        description: The end date filter which returns payroll inputs that are active
          on or before the end date, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: limit
        in: query
        description: The maximum number of objects in a single response. The default
          is 20. The maximum is 100.
        schema:
          type: integer
          format: int64
      - name: offset
        in: query
        description: 'The zero-based index of the first object in a response collection.
          The default is 0. Use offset with the limit parameter to control paging
          of a response collection. Example: If limit is 5 and offset is 9, the response
          returns a collection of 5 objects starting with the 10th object.'
        schema:
          type: integer
          format: int64
      - name: payComponent
        in: query
        description: The Workday ID of the pay component for the payroll input. You
          can use a returned id from GET /values/payrollInputsGroup/payComponents.
        style: form
        explode: true
        schema:
          type: array
          items:
            type: string
      - name: startDate
        in: query
        description: The start date filter which returns payroll inputs that are active
          on or after the start date, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: worker
        in: query
        description: The Workday ID of worker for the payroll input. You can use a
          returned id from GET /workers in the Staffing service.
        style: form
        explode: true
        schema:
          type: array
          items:
            type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payrollInputView'
                  total:
                    type: integer
                    format: int64
                description: collection something or other
        400:
          description: |-
            Invalid request. (https://community.workday.com/rest/error-messages)
            Error Code | Validation Message
            --- | ---
            A552 | Specify a Start Date.
            A116 | Specify these payroll worktag types for [pc]: [worktags].
            A119 | Specify an End Date that is on or after the Start Date.
            A551 | Specify a Related Calculation that's valid for [pc].
            A137 | Specify valid run categories. Invalid run categories are: [runcats]
            A560 | Specify a position for [pc].
            A548 | Specify a Pay Component configured with the correct Worker country.
            A550 | Specify a Type for each Input Details row.
            A559 | Specify an End Date for One-time Payroll Input.
            A139 | Specify worktags valid for payroll. Invalid worktags are: [worktags].
            A555 | Specify a Pay Component.
            A98 | Specify only 1 Worktag for each worktag type.
            A138 | Specify a Position that's valid for [worker].
            A940 | Specify a valid level 1 project plan phase. This phase isn't supported: [phase]
            A142 | Enable Input Allowed on [pc] - [pcrc].
            A141 | Specify only 1 Input Details row for each input Type.
            A120 | Enable Input Amount Allowed on [pc].
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
    post:
      tags:
      - payrollInputs
      summary: Creates payroll inputs.
      operationId: createPayrollInputs
      description: "Creates a single payroll input instance with the specified data.\
        \ In the request body, specify at least these required fields: worker, payComponent\
        \ and startDate.\n\n* If you specified a position-based payComponent, then\
        \ the position field is required.   \n\n* If a worktag applies to the payComponent,\
        \ provide a valid worktag.  \n\n* If ongoing is false, then an endDate is\
        \ required for a one-time payroll input.   \n\n* If you specified inputDetails,\
        \ then the type field is required.   \n\nThe Pay Component Segment Security\
        \ configuration is required. Note that Pay Component Segment Security is supported\
        \ only on the Self-Service: Payroll Public API (Payroll Input) security domain.\
        \ The Segment-Based Security Group must use a <b>Self-Service – Worker</b>\
        \ security group type as the basis.\n\nSecured by: Self-Service: Payroll Public\
        \ API (Payroll Input), Worker Data: Payroll Public API (Payroll Input)\n\n\
        Scope: Core Payroll"
      parameters:
      - name: endDate
        in: query
        description: The end date filter which returns payroll inputs that are active
          on or before the end date, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: payComponent
        in: query
        description: The Workday ID of the pay component for the payroll input. You
          can use a returned id from GET /values/payrollInputsGroup/payComponents.
        style: form
        explode: true
        schema:
          type: array
          items:
            type: string
      - name: startDate
        in: query
        description: The start date filter which returns payroll inputs that are active
          on or after the start date, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: worker
        in: query
        description: The Workday ID of worker for the payroll input. You can use a
          returned id from GET /workers in the Staffing service.
        style: form
        explode: true
        schema:
          type: array
          items:
            type: string
      requestBody:
        content:
          'application/json':
            schema:
              $ref: '#/components/schemas/payrollInputView'
        required: true
      responses:
        201:
          description: Resource created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/payrollInputView'
        400:
          description: |-
            Invalid request. (https://community.workday.com/rest/error-messages)
            Error Code | Validation Message
            --- | ---
            A552 | Specify a Start Date.
            A116 | Specify these payroll worktag types for [pc]: [worktags].
            A119 | Specify an End Date that is on or after the Start Date.
            A551 | Specify a Related Calculation that's valid for [pc].
            A137 | Specify valid run categories. Invalid run categories are: [runcats]
            A560 | Specify a position for [pc].
            A548 | Specify a Pay Component configured with the correct Worker country.
            A550 | Specify a Type for each Input Details row.
            A559 | Specify an End Date for One-time Payroll Input.
            A139 | Specify worktags valid for payroll. Invalid worktags are: [worktags].
            A555 | Specify a Pay Component.
            A98 | Specify only 1 Worktag for each worktag type.
            A549 | Specify a Worker.
            A138 | Specify a Position that's valid for [worker].
            A940 | Specify a valid level 1 project plan phase. This phase isn't supported: [phase]
            A142 | Enable Input Allowed on [pc] - [pcrc].
            A141 | Specify only 1 Input Details row for each input Type.
            A120 | Enable Input Amount Allowed on [pc].
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
      x-codegen-request-body-name: payrollInputView
  /values/taxRatesGroup/stateInstances/:
    get:
      tags:
      - Prompt Values
      description: Retrieves instances that can be used as values for other endpoint
        parameters in this service.
      operationId: getStateInstancesForEndpointParameters
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/MULTIPLE_INSTANCE_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /taxRates:
    get:
      tags:
      - taxRates
      summary: Retrieves a single or a collection of company SUI rates.
      operationId: getCompanySUIRates
      description: |-
        Retrieves all company SUI rates. You can filter the company SUI rates by company, payroll state authority tax code, or effective date.

        Secured by: Process: Tax Filing/W-2s (Run) - USA

        Scope: USA Payroll
      parameters:
      - name: company
        in: query
        description: 'The company reference ID or WID that represents 1 or more companies.
          Example: company=comp1&company=comp2&company=cb550da820584750aae8f807882fa79a'
        schema:
          type: string
      - name: effective
        in: query
        description: The effective date for the SUI rate, using the yyyy-mm-dd format.
        schema:
          type: string
          format: date
      - name: limit
        in: query
        description: The maximum number of objects in a single response. The default
          is 20. The maximum is 100.
        schema:
          type: integer
          format: int64
      - name: offset
        in: query
        description: 'The zero-based index of the first object in a response collection.
          The default is 0. Use offset with the limit parameter to control paging
          of a response collection. Example: If limit is 5 and offset is 9, the response
          returns a collection of 5 objects starting with the 10th object.'
        schema:
          type: integer
          format: int64
      - name: payrollStateAuthorityTaxCode
        in: query
        description: 'The FIPS code or WID that represents 1 or more states. Example:
          payrollStateAuthorityTaxCode=06&payrollStateAuthorityTaxCode=3b3d378d5f4a48b8b3ac46fee0703226&payrollStateAuthorityTaxCode=48'
        schema:
          type: string
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/suiRatesSummary'
                  total:
                    type: integer
                    format: int64
                description: collection something or other
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
    post:
      tags:
      - taxRates
      summary: Creates a single or a collection of Company SUI Rates.
      operationId: createCompanySUIRates
      description: "Creates a single or a collection of company SUI rates. To create\
        \ a single company SUI rate, specify the single JSON object in the request\
        \ body.\nTo create multiple company SUI rates, specify \"bulk=true\" as a\
        \ query parameter. You must specify the JSON object collection in data[ ]\
        \ in the request body. The maximum collection size is 100. \nIn the request\
        \ body, specify at least these required fields: companyInstance, stateInstance,\
        \ taxCode, startDate.\n\nSecured by: Process: Tax Filing/W-2s (Run) - USA\n\
        \nScope: USA Payroll"
      parameters:
      - name: company
        in: query
        description: 'The company reference ID or WID that represents 1 or more companies.
          Example: company=comp1&company=comp2&company=cb550da820584750aae8f807882fa79a'
        schema:
          type: string
      - name: effective
        in: query
        description: The effective date for the SUI rate, using the yyyy-mm-dd format.
        schema:
          type: string
          format: date
      - name: payrollStateAuthorityTaxCode
        in: query
        description: 'The FIPS code or WID that represents 1 or more states. Example:
          payrollStateAuthorityTaxCode=06&payrollStateAuthorityTaxCode=3b3d378d5f4a48b8b3ac46fee0703226&payrollStateAuthorityTaxCode=48'
        schema:
          type: string
      requestBody:
        content:
          'application/json':
            schema:
              $ref: '#/components/schemas/companySuiRateDetails'
        required: true
      responses:
        201:
          description: Resource created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/companySuiRateDetails'
        400:
          description: |-
            Invalid request. (https://community.workday.com/rest/error-messages)
            Error Code | Validation Message
            --- | ---
            A124 | Enter a Start Date that's not between the start and end dates of an existing row. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
            A119 | Enter an End Date that's after or on the Start Date. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
            A12 | Enter an Applicable Rate that's less than 10, contains up to 6 decimal places, and is a positive number. Example: 9.999999. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
            A121 | Enter an End Date that's not between the start and end dates of another row. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
            A12 | You must enter exactly 'W_SUIER' as the taxCode. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
            A12 | Enter an Employer Identification Number that's valid, and isn't null or 'null'. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
            A5 | Enter a U.S. State that's not Guam. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
            A5 | Enter a U.S. company with a U.S. Tax ID. Request Element Values - Company: [company] ([companyWID]), State: [state] ([stateWID]), Start Date: ([start])
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
      x-codegen-request-body-name: companySuiRateDetails
  /values/payrollInputsGroup/payComponents/:
    get:
      tags:
      - Prompt Values
      description: Retrieves instances that can be used as values for other endpoint
        parameters in this service.
      operationId: getPayComponentsForEndpoints
      parameters:
      - name: payComponent
        in: query
        description: The pay component for the worktags. You can use a returned id
          from GET/values/payrollInputsGroup/payComponents.
        schema:
          type: string
      - name: worker
        in: query
        description: The Workday ID of the worker. You can use a returned id from
          GET /workers in the Staffing service.
        schema:
          type: string
      - name: startDate
        in: query
        description: The start date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      - name: endDate
        in: query
        description: The end date for the positions, using the date format yyyy-mm-dd.
        schema:
          type: string
          format: date
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/MULTIPLE_INSTANCE_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            'application/json':
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
  /payGroups:
    get:
      tags:
      - payGroups
      summary: Retrieves a collection of pay groups.
      operationId: getCollectionOfPayGroups
      description: |-
        Retrieves all pay groups. Inactive and External Pay Groups are not supported.

        Secured by: Set Up: Payroll - Pay Group Specific

        Scope: Core Payroll
      parameters:
      - name: country
        in: query
        description: The Workday ID of the country or territory of the pay group.
          You can use a returned country id from any of the payGroups endpoints in
          this Payroll API.
        schema:
          type: string
      - name: limit
        in: query
        description: The maximum number of objects in a single response. The default
          is 20. The maximum is 100.
        schema:
          type: integer
          format: int64
      - name: offset
        in: query
        description: 'The zero-based index of the first object in a response collection.
          The default is 0. Use offset with the limit parameter to control paging
          of a response collection. Example: If limit is 5 and offset is 9, the response
          returns a collection of 5 objects starting with the 10th object.'
        schema:
          type: integer
          format: int64
      responses:
        200:
          description: Successful response. A successful response can return no matched
            data.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/payGroupViewDetail'
                  total:
                    type: integer
                    format: int64
                description: collection something or other
        400:
          description: Invalid request. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        401:
          description: Invalid resource or operation. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        403:
          description: User has insufficient permissions. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        404:
          description: Resource not found. (https://community.workday.com/rest/error-messages)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VALIDATION_ERROR_MODEL_REFERENCE'
        default:
          description: An error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
components:
  schemas:
    INSTANCE_MODEL_REFERENCE:
      required:
      - id
      type: object
      properties:
        id:
          pattern: ^(?:(?:[0-9a-f]{32})|(?:[0-9]+\$[0-9]+)|(\S+=\S+))$
          type: string
          description: wid / id / reference id
        descriptor:
          type: string
          description: A description of the instance
          readOnly: true
        href:
          type: string
          description: A link to the instance
          readOnly: true
    ERROR_MODEL_REFERENCE:
      required:
      - error
      type: object
      properties:
        error:
          type: string
          description: A description of the error
    VALIDATION_ERROR_MODEL_REFERENCE:
      allOf:
      - $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
      - type: object
        properties:
          errors:
            type: array
            description: An array of validation errors
            items:
              $ref: '#/components/schemas/ERROR_MODEL_REFERENCE'
    FACETS_MODEL_REFERENCE:
      type: array
      items:
        type: object
        properties:
          descriptor:
            type: string
            description: A description of the facet
          facetParameter:
            type: string
            description: The alias used to select the facet
          values:
            type: array
            description: the facet values
            items:
              required:
              - id
              type: object
              properties:
                count:
                  type: integer
                  description: The number of instances returned by this facet
                  format: int32
                id:
                  pattern: ^(?:(?:[0-9a-f]{32})|(?:[0-9]+\$[0-9]+)|(\S+=\S+))$
                  type: string
                  description: wid / id / reference id
                descriptor:
                  type: string
                  description: A description of the facet
                href:
                  type: string
                  description: A link to the instance
                  readOnly: true
        description: This object represents the possible facets for this resource
        readOnly: true
    MULTIPLE_INSTANCE_MODEL_REFERENCE:
      type: object
      properties:
        total:
          type: integer
        data:
          type: array
          items:
            $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
    payComponent:
      allOf:
      - $ref: '#/components/schemas/payComponentDetail'
      - type: object
    currency:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    position:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    worker_dc:
      allOf:
      - $ref: '#/components/schemas/workerDetail'
      - type: object
    payrollInputView:
      allOf:
      - type: object
        properties:
          runCategories:
            type: array
            description: The run category for the payroll input.
            items:
              $ref: '#/components/schemas/runCategoryDetail'
            x-workday-type: Multi-instance
          payComponent:
            $ref: '#/components/schemas/payComponent'
          comment:
            type: string
            description: The text comment for this input.
            example: Sample Data for Payroll Input
            x-workday-type: Text
          startDate:
            type: string
            description: The start date before which this input does not apply.
            format: date
            x-workday-type: Date
          currency:
            $ref: '#/components/schemas/currency'
          worktags:
            type: array
            description: The worktags associated with the payroll input.
            items:
              $ref: '#/components/schemas/worktag'
            x-workday-type: Multi-instance
          inputDetails:
            type: array
            description: The details for this payroll input.
            items:
              $ref: '#/components/schemas/inputLine'
            x-workday-type: Multi-instance
          ongoing:
            type: boolean
            description: If true, the payroll input is ongoing.
            example: true
            x-workday-type: Boolean
          position:
            $ref: '#/components/schemas/position'
          fieldEditability:
            type: string
            description: 'The editability status indicating the fields that can be
              updated in the payroll input request. Possible values: all, none, endDateOnly'
            example: Lorem ipsum dolor sit amet, cum choro singulis consectetuer ut,
              ubique iisque contentiones ex duo. Quo lorem etiam eu.
            x-workday-type: Text
          endDate:
            type: string
            description: The end date after which this input does not apply.
            format: date
            x-workday-type: Date
          worker:
            $ref: '#/components/schemas/worker_dc'
          adjustment:
            type: boolean
            description: If true, the input is for an adjustment as opposed to an
              override.
            example: true
            x-workday-type: Boolean
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    country:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    payGroupViewDetail:
      allOf:
      - type: object
        properties:
          country:
            $ref: '#/components/schemas/country'
          payGroupDetails:
            type: array
            description: Contains the pay group detail.
            items:
              $ref: '#/components/schemas/payGroupDetailMiniViewDetail'
            x-workday-type: Multi-instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    stateInstance:
      allOf:
      - $ref: '#/components/schemas/stateDetail'
      - type: object
    companyInstance:
      allOf:
      - $ref: '#/components/schemas/companyDetail'
      - type: object
    suiRatesSummary:
      allOf:
      - type: object
        properties:
          id:
            type: string
            description: Unique identifier for company SUI rate.
            example: d79877b2537d017d2961438c11005003
            x-workday-type: Text
          ein:
            type: string
            description: The EIN value for the company SUI rate.
            example: 604-8144-7
            x-workday-type: Text
          startDate:
            type: string
            description: The start date value of the row for company SUI rate.
            format: date
            x-workday-type: Date
          exempt:
            type: boolean
            description: If true, the SUI rate is exempt.
            example: true
            x-workday-type: Boolean
          endDate:
            type: string
            description: The end date value of the row for company SUI rate.
            example: 2020-01-01
            x-workday-type: Text
          einType:
            type: string
            description: 'The EIN type. Valid values: SUI EIN, STATE EIN, FEIN.'
            example: STATE EIN
            x-workday-type: Text
          rateType:
            type: string
            description: 'The rate type. Valid values: OR for override rate, DR for
              default rate.'
            example: OR
            x-workday-type: Text
          taxCode:
            maxLength: 20
            type: string
            description: The payroll tax code. As of v1, the default value is W_SUIER.
            example: W_SUIER
            x-workday-type: Text
          applicableRate:
            type: string
            description: The applicable rate for the company SUI.
            example: "0.028"
            x-workday-type: Text
          stateInstance:
            $ref: '#/components/schemas/stateInstance'
          companyInstance:
            $ref: '#/components/schemas/companyInstance'
    companyInstanceDetail:
      allOf:
      - $ref: '#/components/schemas/companyDetail2'
      - type: object
    stateInstanceDetail:
      allOf:
      - $ref: '#/components/schemas/state2'
      - type: object
    companySuiRateDetails:
      allOf:
      - required:
        - companyInstance
        - startDate
        - stateInstance
        - taxCode
        type: object
        properties:
          companyInstance:
            $ref: '#/components/schemas/companyInstanceDetail'
          stateInstance:
            $ref: '#/components/schemas/stateInstanceDetail'
          exempt:
            type: boolean
            description: If true, the SUI rate is exempt.
            example: true
            x-workday-type: Boolean
          startDate:
            type: string
            description: The start date for company tax reporting.
            format: date
            x-workday-type: Date
          taxCode:
            maxLength: 20
            type: string
            description: The deduction for company tax reporting.
            example: W_SUIER
            x-workday-type: Text
          ein:
            type: string
            description: The payroll tax authority EIN field for company tax reporting.
            example: 604-8144-7
            x-workday-type: Text
          endDate:
            type: string
            description: The end date for company tax reporting.
            format: date
            x-workday-type: Date
          applicableRate:
            type: string
            description: The tax override rate for company tax reporting.
            example: "9.9"
            x-workday-type: Text
          id:
            type: string
            description: Id of the instance
    runCategory:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    currentPeriodInProgress:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    nextPeriodToProcess:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    firstProcessingPeriod:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    lastPeriodCompleted:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    periodSchedule:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    payGroup:
      allOf:
      - $ref: '#/components/schemas/payGroupSimpleViewDetail'
      - type: object
    payGroupDetailView:
      allOf:
      - type: object
        properties:
          runCategory:
            $ref: '#/components/schemas/runCategory'
          currentPeriodInProgress:
            $ref: '#/components/schemas/currentPeriodInProgress'
          nextPeriodToProcess:
            $ref: '#/components/schemas/nextPeriodToProcess'
          firstProcessingPeriod:
            $ref: '#/components/schemas/firstProcessingPeriod'
          lastPeriodCompleted:
            $ref: '#/components/schemas/lastPeriodCompleted'
          periodSchedule:
            $ref: '#/components/schemas/periodSchedule'
          payRunGroup:
            type: array
            description: Contains one or more pay groups that are processed or run
              at the same time.
            items:
              $ref: '#/components/schemas/payRunGroupSimpleViewDetail'
            x-workday-type: Multi-instance
          payGroup:
            $ref: '#/components/schemas/payGroup'
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    location:
      allOf:
      - $ref: '#/components/schemas/locationDataJobView'
      - type: object
    worker:
      allOf:
      - $ref: '#/components/schemas/workerJobView'
      - type: object
    supervisoryOrganization:
      allOf:
      - $ref: '#/components/schemas/supervisoryOrganizationJobView'
      - type: object
    jobType:
      allOf:
      - $ref: '#/components/schemas/jobTypeData'
      - type: object
    jobProfile:
      allOf:
      - $ref: '#/components/schemas/jobProfileJobView'
      - type: object
    jobData:
      allOf:
      - type: object
        properties:
          location:
            $ref: '#/components/schemas/location'
          businessTitle:
            type: string
            description: The business title for the position.
            example: Sample Business Title
            x-workday-type: Text
          worker:
            $ref: '#/components/schemas/worker'
          supervisoryOrganization:
            $ref: '#/components/schemas/supervisoryOrganization'
          nextPayPeriodStartDate:
            type: string
            description: The next pay period start date for the job.
            format: date
            x-workday-type: Date
          jobType:
            $ref: '#/components/schemas/jobType'
          jobProfile:
            $ref: '#/components/schemas/jobProfile'
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    workerJobView:
      allOf:
      - type: object
        properties:
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    payGroupSimpleViewDetail:
      allOf:
      - type: object
        properties:
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    runCategory2:
      allOf:
      - $ref: '#/components/schemas/INSTANCE_MODEL_REFERENCE'
      - type: object
    payGroupDetailMiniViewDetail:
      allOf:
      - type: object
        properties:
          runCategory:
            $ref: '#/components/schemas/runCategory2'
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    companyDetail:
      allOf:
      - type: object
        properties:
          company:
            type: string
            description: The Reference ID to use for lookups within our Workday Web
              Services. For supervisory organizations, this is also the Organization_ID.
            example: GMS_USA_company
            x-workday-type: Text
          fein:
            type: string
            description: The FEIN for the US company.
            example: 99-0343105
            x-workday-type: Text
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    type_:
      allOf:
      - $ref: '#/components/schemas/inputInterfaceDetail'
      - type: object
    inputLine:
      allOf:
      - type: object
        properties:
          type:
            $ref: '#/components/schemas/type_'
          value:
            type: integer
            description: The value for the input line.
            example: 213433614
            x-workday-type: Numeric
          id:
            type: string
            description: Id of the instance
    companyDetail2:
      allOf:
      - type: object
        properties:
          id:
            type: string
            description: Id of the instance
    runCategoryDetail:
      allOf:
      - type: object
        properties:
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    jobTypeData:
      allOf:
      - type: object
        properties:
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    countryDataForLocationData:
      allOf:
      - type: object
        properties:
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    worktag:
      allOf:
      - type: object
        properties:
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    payRunGroupSimpleViewDetail:
      allOf:
      - type: object
        properties:
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    inputInterfaceDetail:
      allOf:
      - type: object
        properties:
          name:
            type: string
            description: The alternate ID of the related calculation for the pay component
              and pay component related calculation.
            example: Lorem ipsum dolor sit amet, cum choro singulis consectetuer ut,
              ubique iisque contentiones ex duo. Quo lorem etiam eu.
            x-workday-type: Text
          id:
            type: string
            description: Id of the instance
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
    workerDetail:
      allOf:
      - type: object
        properties:
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    payComponentDetail:
      allOf:
      - type: object
        properties:
          code:
            maxLength: 20
            type: string
            description: The payroll code of the pay component.
            example: Lorem ipsum dolor sit amet, cum choro singulis consectetuer ut,
              ubique iisque contentiones ex duo. Quo lorem etiam eu.
            x-workday-type: Text
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    jobProfileJobView:
      allOf:
      - type: object
        properties:
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    state2:
      allOf:
      - type: object
        properties:
          id:
            type: string
            description: Id of the instance
    country_:
      allOf:
      - $ref: '#/components/schemas/countryDataForLocationData'
      - type: object
    locationDataJobView:
      allOf:
      - type: object
        properties:
          country:
            $ref: '#/components/schemas/country_'
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    stateDetail:
      allOf:
      - type: object
        properties:
          payrollStateAuthorityTaxCode:
            type: string
            description: The Payroll Authority Tax Code for a Payroll Tax Authority.
            example: "06"
            x-workday-type: Text
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
    supervisoryOrganizationJobView:
      allOf:
      - type: object
        properties:
          descriptor:
            type: string
            description: A preview of the instance
            example: Lorem ipsum dolor sit ame
          id:
            type: string
            description: Id of the instance
  securitySchemes:
    OAuth2:
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://tenantauthorizationhostname
          scopes: {}
